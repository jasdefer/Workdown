# Metrics

| Label | Value |
| --- | --- |
| Total items | 72 |
| To do | 11 |
| In progress | 3 |
| Done | 58 |
| Sum of scheduled duration | 25w 1d |
| Earliest start | 2026-04-20 |
| Latest end | 2026-07-31 |

> _189 items dropped:_
> _- "Sum of scheduled duration" missing 'duration': "ADR — visualization architecture", "Compute schema-declared aggregate fields up the parent chain", "App shell navigation (views menu + future link slots)", "Audit workdown add for UI-driven creation", "workdown body — replace the Markdown body", "workdown move — shortcut for the board field", "workdown rename — change an item's id", "workdown set — replace a field value", "workdown set — type-aware modes (append, remove, delta)", "workdown unset — clear a field", "Add `color` field type with background tinting", "Relocate cross-cutting helpers out of feature modules", "Make diagnostic source-routing structural, not enumerative", "Collapse parallel View* slot variants and unify their validation helpers", "Cross-field comparison rule for duration values", "Add `duration` field type", "Explicit `in` operator; `=` becomes always-literal", "Store FieldValue::Date as chrono::NaiveDate", "First view end-to-end (board, read-only)", "Consolidate duplication and tighten types before more foundation work", "Gantt duration input mode", "Gantt predecessor input mode", "Default generator that reads dates from git history", "File watcher and SSE for live updates", "Design multi-project support — set decisions and break out follow-up work", "Multi-project support", "Mutations end-to-end", "Remaining read-only views", "Bar chart renderer", "Board renderer", "workdown render command", "Gantt renderer", "Gantt by depth view", "Gantt by initiative view", "Graph renderer", "Heatmap renderer", "Line chart renderer", "Metric renderer", "Render module hygiene — escape helpers, test fixtures, common.rs naming", "Table renderer", "Tree renderer", "Treemap renderer", "Workload renderer", "Validate resource references and render resource pickers", "Rules can't reference the current date", "Expose schema metadata so the UI can offer valid choices", "Make store-diagnostic surfacing consistent across commands", "Time tracking", "UI foundation — conventions and scaffolding before the first view", "Create a new view from the UI", "Design ViewData and extractors", "Per-view-kind display configuration (which fields show where)", "Build and edit a view's where filter from the UI", "Persist view definitions to views.yaml", "Add views path to config; ship default views.yaml", "Cross-file validation for views.yaml", "Editor-only JSON Schema for views.yaml", "Add per-view `title:` slot to views.yaml", "Wire views validation into workdown validate", "Design views.yaml shape", "Unify the upward chain walks and link-target reads", "workdown serve skeleton with embedded UI", "Split into core / cli / server workspace"_
> _- "Earliest start" missing 'start_date': "ADR — visualization architecture", "Compute schema-declared aggregate fields up the parent chain", "App shell navigation (views menu + future link slots)", "Audit workdown add for UI-driven creation", "workdown body — replace the Markdown body", "workdown move — shortcut for the board field", "workdown rename — change an item's id", "workdown set — replace a field value", "workdown set — type-aware modes (append, remove, delta)", "workdown unset — clear a field", "Add `color` field type with background tinting", "Relocate cross-cutting helpers out of feature modules", "Make diagnostic source-routing structural, not enumerative", "Collapse parallel View* slot variants and unify their validation helpers", "Cross-field comparison rule for duration values", "Add `duration` field type", "Explicit `in` operator; `=` becomes always-literal", "Store FieldValue::Date as chrono::NaiveDate", "First view end-to-end (board, read-only)", "Consolidate duplication and tighten types before more foundation work", "Gantt duration input mode", "Gantt predecessor input mode", "Default generator that reads dates from git history", "File watcher and SSE for live updates", "Design multi-project support — set decisions and break out follow-up work", "Multi-project support", "Mutations end-to-end", "Remaining read-only views", "Bar chart renderer", "Board renderer", "workdown render command", "Gantt renderer", "Gantt by depth view", "Gantt by initiative view", "Graph renderer", "Heatmap renderer", "Line chart renderer", "Metric renderer", "Render module hygiene — escape helpers, test fixtures, common.rs naming", "Table renderer", "Tree renderer", "Treemap renderer", "Workload renderer", "Validate resource references and render resource pickers", "Rules can't reference the current date", "Expose schema metadata so the UI can offer valid choices", "Make store-diagnostic surfacing consistent across commands", "Time tracking", "UI foundation — conventions and scaffolding before the first view", "Create a new view from the UI", "Design ViewData and extractors", "Per-view-kind display configuration (which fields show where)", "Build and edit a view's where filter from the UI", "Persist view definitions to views.yaml", "Add views path to config; ship default views.yaml", "Cross-file validation for views.yaml", "Editor-only JSON Schema for views.yaml", "Add per-view `title:` slot to views.yaml", "Wire views validation into workdown validate", "Design views.yaml shape", "Unify the upward chain walks and link-target reads", "workdown serve skeleton with embedded UI", "Split into core / cli / server workspace"_
> _- "Latest end" missing 'end_date': "ADR — visualization architecture", "Compute schema-declared aggregate fields up the parent chain", "App shell navigation (views menu + future link slots)", "Audit workdown add for UI-driven creation", "workdown body — replace the Markdown body", "workdown move — shortcut for the board field", "workdown rename — change an item's id", "workdown set — replace a field value", "workdown set — type-aware modes (append, remove, delta)", "workdown unset — clear a field", "Add `color` field type with background tinting", "Relocate cross-cutting helpers out of feature modules", "Make diagnostic source-routing structural, not enumerative", "Collapse parallel View* slot variants and unify their validation helpers", "Cross-field comparison rule for duration values", "Add `duration` field type", "Explicit `in` operator; `=` becomes always-literal", "Store FieldValue::Date as chrono::NaiveDate", "First view end-to-end (board, read-only)", "Consolidate duplication and tighten types before more foundation work", "Gantt duration input mode", "Gantt predecessor input mode", "Default generator that reads dates from git history", "File watcher and SSE for live updates", "Design multi-project support — set decisions and break out follow-up work", "Multi-project support", "Mutations end-to-end", "Remaining read-only views", "Bar chart renderer", "Board renderer", "workdown render command", "Gantt renderer", "Gantt by depth view", "Gantt by initiative view", "Graph renderer", "Heatmap renderer", "Line chart renderer", "Metric renderer", "Render module hygiene — escape helpers, test fixtures, common.rs naming", "Table renderer", "Tree renderer", "Treemap renderer", "Workload renderer", "Validate resource references and render resource pickers", "Rules can't reference the current date", "Expose schema metadata so the UI can offer valid choices", "Make store-diagnostic surfacing consistent across commands", "Time tracking", "UI foundation — conventions and scaffolding before the first view", "Create a new view from the UI", "Design ViewData and extractors", "Per-view-kind display configuration (which fields show where)", "Build and edit a view's where filter from the UI", "Persist view definitions to views.yaml", "Add views path to config; ship default views.yaml", "Cross-file validation for views.yaml", "Editor-only JSON Schema for views.yaml", "Add per-view `title:` slot to views.yaml", "Wire views validation into workdown validate", "Design views.yaml shape", "Unify the upward chain walks and link-target reads", "workdown serve skeleton with embedded UI", "Split into core / cli / server workspace"_
